## Scripts used to generate `data/`

Note that training the models does not require running these scripts. They are only
provided "as is" in order to help reproduce the content of the `data/` subdirectory.
